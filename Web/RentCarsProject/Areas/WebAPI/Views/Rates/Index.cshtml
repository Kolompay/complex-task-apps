@model IEnumerable<RentCarsProject.Rate>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Idcar)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CarIdcarNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Idcar)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CarIdcarNavigation.Brand)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idrate">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idrate">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idrate">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
